IP_ADDRESS = "138.4.46.83"
PORT_SOCKET = 30000
PORT_SERIAL = "COM6"

#Se ha obtenido el numero de estacion con el programa TeleTrans
NUMERO_ESTACION = 2171
NUMERO_USUARIO = 1

myEstacion = Estacion.Estacion(NUMERO_ESTACION)

#A continuación se definen los parámetros necesarios para la comunicación, hay que tener en cuenta que la comunicación serie tiene preferencia,
# si se define un puerto serie de comunicación el programa intentará comunicarse por serie.
#Para evitar esto, si se ha definido ya un puerto de comunicación serie, hay que ejecutar la siguiente instrucción:
#	myEstacion.set_serial_port()
myEstacion.set_address(IP_ADDRESS, PORT_SOCKET) #Se le indica la ip y el puerto para la comunicación con la estación
myEstacion.set_serial_port(PORT_SERIAL) #Se indica el puerto serie por el que se realizará la comunicación

medidas_inst = myEstacion.medidas_instantaneas(NUMERO_USUARIO)
medidas_no_inst = myEstacion.medidas_no_intantaneas(NUMERO_USUARIO, 14)
